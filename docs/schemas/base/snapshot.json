{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/lachlan-ibm/verify-access-configurator/schemas/base/snapshot.json",
  "description": "A snapshot can be applied to both Container and Appliance deployments to restore a previous configuration state. This is done via a signed archive file, generated by the deployment you are trying to preserve/re-create.",
  "type": "object",
  "properties": {
    "snapshot": {
      "type": "string",
      "description": "Path to the signed snapshot archive file to apply.",
      "pattern": ".*\\.snapshot$"
    }
  },
  "required": [
    "snapshot"
  ],
  "examples": [
    {
        "snapshot": "snapshots/isva-2023-02-08.snapshot"
    }
  ]
}
